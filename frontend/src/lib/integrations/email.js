// Resend / SendGrid email gönderimi
// POST /api/orders/{id}/send backend endpoint'i burayı çağıracak.

// sendOrderEmail(to, order) → provider response
export async function sendOrderEmail() {
    throw new Error("Email provider not yet configured");
}
