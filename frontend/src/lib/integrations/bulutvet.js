// BulutVet REST API
// Veteriner klinik otomasyonu için — veteriner modu aktifleştiğinde kullanılır.

// syncInventory(apiKey) → list of { gtin, current_stock }
export async function syncInventory() {
    throw new Error("BulutVet integration not yet configured");
}
