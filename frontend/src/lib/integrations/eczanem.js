// Eczanem / İlon otomasyon yazılımı
// SOAP entegrasyonu — müşteri webservice URL'ini ve credentials'ı girer.
// Bu dosya planlandığında aşağıdaki fonksiyonlar implement edilecek.

// pullInventory(wsUrl, credentials) → list of { gtin, current_stock }
export async function pullInventory() {
    throw new Error("Eczanem integration not yet configured");
}

// pushOrder(wsUrl, credentials, order) → supplier response
export async function pushOrder() {
    throw new Error("Eczanem integration not yet configured");
}
